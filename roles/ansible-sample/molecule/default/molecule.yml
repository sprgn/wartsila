---
dependency:
  name: galaxy
driver:
  name: docker
platforms:
  - name: molecule-default
    image: molecule-default
    dockerfile: ./Dockerfile
    volumes:
      - /sys/fs/cgroup:/sys/fs/cgroup:ro
    privileged: true
provisioner:
  name: ansible
  lint:
    name: ansible-lint
verifier:
  name: ansible
